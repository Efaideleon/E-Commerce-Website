/**
 * Content to show on the dashboard
 * @member total_spent - The sum from each inventory table `total_spent`
 */
export type InventoryDashBoardProps = {
    total_spent: number;
    total_enviado: number;
}